{
  "name": "heif-to-jpeg-converter",
  "version": "1.0.0",
  "description": "Browser-based HEIF/HEIC to JPEG converter — no server required",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "libheif-js": "^1.18.2"
  }
}
